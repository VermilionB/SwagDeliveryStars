import React from 'react';

interface SmallBeatCardComponentProps {
    beat: {}
}
const SmallBeatCardComponent = ({}) => {
    return (
        <div>

        </div>
    );
};

export default SmallBeatCardComponent;